// Generated by CoffeeScript 1.6.2
(function() {
  var _ref,
    __bind = function(fn, me) {
      return function() {
        return fn.apply(me, arguments);
      };
    },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) {
      for (var key in parent) {
        if (__hasProp.call(parent, key)) child[key] = parent[key];
      }

      function ctor() {
        this.constructor = child;
      }
      ctor.prototype = parent.prototype;
      child.prototype = new ctor();
      child.__super__ = parent.prototype;
      return child;
    };

  Dashing.BeachFlag = (function(_super) {
    __extends(BeachFlag, _super);

    function BeachFlag() {
      this.refreshData = __bind(this.refreshData, this);
      _ref = BeachFlag.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    BeachFlag.prototype.ready = function() {
      this.refreshData();
      return setInterval(this.refreshData, 60000 * 60 * 2); // every 2 hours
    };

    BeachFlag.prototype.refreshData = function() {
      var url = '/beach/' + this.get('location') + '/status.json';
      var that = this;
      $.getJSON(url, function(data) {
        that.showData(data);
      });
    };

    BeachFlag.prototype.showData = function(data) {
      this.set('beach', data);
    };

    return BeachFlag;

  })(Dashing.Widget);

}).call(this);