// Generated by CoffeeScript 1.6.2
(function() {
  var _ref,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Dashing.WeatherStationVariable = (function(_super) {
    __extends(WeatherStationVariable, _super);

    function WeatherStationVariable() {
      this.refreshData = __bind(this.refreshData, this);      _ref = WeatherStationVariable.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    WeatherStationVariable.prototype.ready = function() {
      $(this.node).addClass(this.get('variable'));
      this.refreshData();
      return setInterval(this.refreshData, 60000);
    };

    WeatherStationVariable.prototype.refreshData = function() {      
      var url = '/weather/' + this.get('location') + '/' + this.get('variable') + '.json';      
      var that = this;
      $.getJSON(url, function(data) {
        that.showData(data);
      });
    };

    WeatherStationVariable.prototype.showData = function(data) {

      /*this.set('current-value', data.current.value + " " + data.inputUnits);      
      this.set('current-time', data.current.time);      
      this.set('min-value', data.min.value + " " + data.inputUnits);      
      this.set('min-time', data.min.time);      
      this.set('max-value', data.max.value + " " + data.inputUnits);      
      this.set('max-time', data.max.time);      
      if (this.get('variable') == 'wind_speed'){
        $(this.node).addClass('wind-direction-' + data.current.wind_from_direction);  
      }      */

      this.set('item',data);
      if (this.get('title') == ''){
        this.set('title', data.display_name);
      }

    };

    return WeatherStationVariable;

  })(Dashing.Widget);

}).call(this);
